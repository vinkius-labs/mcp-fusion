#!/bin/sh
#
# Pre-commit hook: mirrors CI/CD pipeline locally.
# Delegates to Node.js for cross-platform compatibility.
#
# Uses "node --input-type=commonjs" because the project has
# "type": "module" in package.json, but hook scripts cannot
# use .mjs extension and ESM imports fail outside module scope.
#

exec node --input-type=commonjs .githooks/pre-commit.cjs
