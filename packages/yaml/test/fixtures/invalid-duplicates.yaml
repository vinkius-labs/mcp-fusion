version: "1.0"

server:
  name: "duplicate-names"

connections:
  api:
    type: rest
    base_url: "https://api.example.com"

tools:
  - name: get_data
    description: "First tool"
    execute:
      connection: api
      method: GET
      path: "/data"

  - name: get_data
    description: "Duplicate name"
    execute:
      connection: api
      method: GET
      path: "/data2"
